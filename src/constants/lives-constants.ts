/** Default lives when group/student has no stored value (matches DB column defaults). */
export const DEFAULT_STUDENT_LIVES = 3;

/** Max `{ accountId, delta }` rows accepted by bulk lives update. */
export const BULK_UPDATE_LIVES_MAX_STUDENTS = 200;

/** Max IdP metadata XML download size (256 KiB). */
export const IDP_METADATA_FETCH_MAX_BYTES = 256 * 1024;

/** IdP metadata HTTP fetch timeout (milliseconds). */
export const IDP_METADATA_FETCH_TIMEOUT_MS = 15_000;

/** Random bytes for auto-generated enrollment codes (hex length = 2 × this value). */
export const ENROLLMENT_CODE_GENERATED_BYTE_LENGTH = 3;

/** Minimum allowed value for `maxUses` when set explicitly. */
export const ENROLLMENT_CODE_MIN_MAX_USES = 1;

# Listen port (default 8080)
PORT=8080

# Allowed browser `Origin` values (comma-separated, no trailing slash, no path).
# If unset, defaults include the public UI, localhost, and 127.0.0.1 on port 3000.
# If you serve the UI only over HTTPS, add e.g. https://maq.projektstudencki.pl here.
# CORS_ORIGIN=http://maq.projektstudencki.pl,http://localhost:3000,http://127.0.0.1:3000
